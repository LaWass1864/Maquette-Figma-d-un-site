**Badge** — the uppercase eyebrow. Pill form (with accent dot) tags the hero; `bare` form is a plain orange section eyebrow.

```jsx
<Badge>Boxe Anglaise · Amateur Élite</Badge>
<Badge bare>Les Objectifs</Badge>
```

Props: `dot` (toggle the dot on the pill), `bare` (no border/dot, accent text).
